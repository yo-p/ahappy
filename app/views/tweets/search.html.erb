<h1>検索画面</h1>
<%= form_with url: tweets_path, method: :get, local: true do |f| %>
        <%= f.label :genre %>
        <%= f.collection_check_boxes(:genre_ids, Genre.all, :id, :type) do |b| %>
            <%= b.label {b.check_box + b.text} %>
        <% end %>
    <%= f.submit '検索' %>
<% end %>