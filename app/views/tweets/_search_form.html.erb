<%= search_form_for(@q, url:search_tweets_path) do |f| %>
        <%= f.label :genre_ids, 'ジャンル検索' %>
        <%= f.collection_check_boxes(:genre_ids, Genre.all, :id, :type) do |b| %>
            <%= b.label {b.check_box + b.text} %>
        <% end %>
    <%= f.submit :search %>
<% end %>