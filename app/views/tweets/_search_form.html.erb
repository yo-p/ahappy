<%= search_form_for(@q, url:search_path, method: :get) do |f| %>
  <%= f.label :genre_ids_in, 'ジャンル' %>
    <%= f.collection_check_boxes(:genre_id_in, Genre.all, :id, :type)  do |b| %>
      <%= b.label {b.check_box + b.text} %>
    <% end %>
  <%= f.submit '検索する', class:"btn btn-info" %>
<% end %>